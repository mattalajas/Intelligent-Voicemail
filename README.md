# Intelligent-Voicemail

## Run locally

1. `npm install`
2. `npm run dev`
3. Open the local Vite URL shown in the terminal

## Project structure

- `src/App.jsx`: app entry
- `src/features/voicemail/`: voicemail feature modules
- `src/components/ui/`: small local UI primitives
